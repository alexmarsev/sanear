 - implement low-latency rendering path for realtime capture playback
 - implement proper low-latency clock matching (mainly for DVB)
 - make sure that frame-stepping works alright
 - stop wasapi on eos more gracefully

 - maybe some other stuff

 - resolve licensing issues
 - first release

 - detect default audio device modification in exclusive mode
 - don't recreate the device when media type changes during playback and the old one can be used
 - add "excessive precision processing" option
 - play silence during pause in exclusive mode (for ati hdmi)
